version: 0.2

runtime-versions:
  ruby: 2.6

env:
  parameter-store:
    UNLEASH_URI: "epbr-unleash-build-and-test-uri"

phases:
  install:
    runtime-versions:
      ruby: 2.6
    commands:
      - gem install bundler:2.0.2
      - bundle install
  build:
    commands:
      - echo ">>>>>>>> RUNNING TESTS"
      - rake spec