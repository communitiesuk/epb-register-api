version: 0.2

phases:
  install:
    runtime-versions:
      docker: 19
  pre_build:
    commands:
      - aws --version

  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
      - docker build -t ebpr-register-api-image ./

  post_build:
    commands:
      - echo Build completed on `date`
      - docker save -o ebpr-register-api-image.tar ebpr-register-api-image:latest

artifacts:
  files:
    - '**/*'


